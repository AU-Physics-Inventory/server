package edu.andrews.cas.physics.inventory.server.utils;

object Constants {
    const val MIN_PASSWORD_LENGTH: Int = 8;
    const val DEFAULT_TIMEOUT: Long = 900000; // in milliseconds, equivalent to 30 minutes
}